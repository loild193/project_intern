import { makeStyles } from '@material-ui/core';

export const formControlStyle = makeStyles({
	loginInput: {
		marginTop: "20px",
	},
});

export const marginStyle = makeStyles({
	marginTop20px: {
		marginTop: "20px",
	},
});